클래스 내에서 멤버 변수를 초기화 하면 모든 객체들이 멤버를 공유하기 때문에, 객체를 생성할 때마다 static멤버 변수 값이 계속해서 덮어 씌워진다
그래서 static 멤버 변수는 클래스 내에서 선언만 가능하고 정의는 불가능하다
그리고 static 멤버 변수는 모든 객체들이 접근해야 하므로 프로그램 시작부터 끝까지 유지되어야 하기 때문에 전역 범위에서만 정의할 수 있다
따라서 static 멤버 변수는 main함수는 물론이고 생성자 안에서도 초기화 할 수 없다.